function checkSet(){
    // Only change code below this line
    var set = new Set([1, 2, 3, 4, 5]);
    set.delete(2)
    set.delete(5)
    console.log(set)
    // Only change code above this line
    return set;   
  }